# script runner for Remix

## build the script runner
```
yarn && yarn build
```

## run tests
```
cd test
yarn && yarn install
yarn test
``` 
or run a speficic test with
``` 
yarn test --test dist/tests/ts-script.test.js 
```